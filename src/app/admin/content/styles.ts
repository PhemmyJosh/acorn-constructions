/**
 * Shared class strings for the content tabs, so the add/edit forms and tables
 * match the form-submission views rather than drifting into a second style.
 */

export const fieldClasses =
  "rounded-sm border border-acorn-bronze/30 bg-white px-4 py-2.5 text-sm text-acorn-charcoal outline-none focus:border-acorn-gold focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-acorn-gold";

export const labelClasses = "text-sm font-semibold text-acorn-charcoal";

export const panelClasses =
  "rounded-sm border border-acorn-bronze/20 bg-white p-6";

export const primaryButton =
  "inline-flex items-center gap-2 rounded-sm bg-acorn-gold px-5 py-2.5 font-heading text-xs uppercase tracking-[0.15em] text-acorn-charcoal transition-colors hover:brightness-95";

export const secondaryButton =
  "inline-flex items-center gap-2 rounded-sm border border-acorn-bronze/40 px-5 py-2.5 font-heading text-xs uppercase tracking-[0.15em] text-acorn-charcoal transition-colors hover:bg-acorn-stone";

export const tableWrapper =
  "mt-4 overflow-x-auto rounded-sm border border-acorn-bronze/20 bg-white";

export const tableClasses = "min-w-full border-collapse text-left text-sm";

export const theadClasses = "bg-acorn-stone";

export const thClasses =
  "whitespace-nowrap px-3 py-3 font-heading text-[11px] uppercase tracking-[0.12em] text-acorn-charcoal";

export const tdClasses = "px-3 py-3 align-middle text-acorn-charcoal/80";
